DROP TABLE IF EXISTS user;

CREATE TABLE user (
    id_user SERIAL PRIMARY KEY,
    nome VARCHAR(70) NOT NULL,
    ra VARCHAR(7) UNIQUE,
    login VARCHAR(20) UNIQUE NOT NULL,
    senha TEXT NOT NULL,
    ano INT,
    tipo VARCHAR(15) NOT NULL,
    telefone VARCHAR(15),
    bloqueado BOOLEAN NOT NULL DEFAULT FALSE
);

INSERT INTO user VALUES (DEFAULT, 'Estevão Pereira Rolim', '1757075', 'ETVO', '952388fda169f9d66a929b6c95ca7c53', 3, 'Aluno', '14981743304', DEFAULT);

SELECT * FROM user;